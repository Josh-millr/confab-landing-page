import React, { useState, useEffect } from "react";

export const useDimention = async () => {
  let [displayNav, setDisplayNav] = useState(false);
  let [currentWindowWidth, setCurrentWidowWidth] = useState(0);
  useEffect(() => {});
  return [];
};
